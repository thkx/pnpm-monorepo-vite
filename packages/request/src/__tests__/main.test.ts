import request from "../index";

describe("request", () => {
  test("exports", () => {
    expect(request).toBeTruthy();
  });
  test("works", () => {
    // expect(request(1, 2)).toEqual(3);
  });
});
